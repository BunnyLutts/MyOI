#include <cstdio>

using namespace std;

int main(void) {
  freopen("game.in", "r", stdin);
  freopen("game.out", "w", stdout);

  int n, m;
  scanf("%d %d", &n, &m);

  fclose(stdin);
  fclose(stdout);
  return 0;
}
