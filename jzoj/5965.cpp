#include <cstdio>

using namespace std;

int main(void) {
  freopen("game.in", "r", stdin);
  freopen("game.out", "w", stdout);

  int n, m;
  scanf("%d %d", &n, &m);
  for (int i=1; i<=n; i++)

  fclose(stdin);
  fclose(stdout);
  return 0;
}
