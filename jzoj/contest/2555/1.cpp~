#define DEBUG
#include <cstdio>

using namespace std;

int main() {
#ifdef DEBUG
  freopen("1.in", "r", stdin);
  freopen("1.out", "w", stdout);
#endif //DEBUG

  printf("PaPaFish is laying egg!");

  fclose(stdin);
  fclose(stdout);
  return 0;
}
